---
title: The Repository Map
description: Every repository in the Adawy-Group organization — what it is, which world it belongs to, which one to clone for the work in front of you, and where the shared conventions genuinely hold.
tags: ['Reference', 'Start here']
---

# The Repository Map

**Eight repositories.** Knowing which world a repo belongs to tells you which
rules apply to it before you open a single file.

<Stats>
  <Stat value="8" label="repositories" hint="in the Adawy-Group org" />
  <Stat value="1" label="Group monorepo" hint="every brand site" />
  <Stat value="5" label="client projects" hint="one repo each" />
  <Stat value="7" label="bilingual" hint="all but this handbook" />
</Stats>

| Repository | World | What it is | Stack |
| ---------- | ----- | ---------- | ----- |
| [`adawy-platform`](/repositories/adawy-platform) | <Badge tone="group">Group</Badge> | The monorepo behind every Adawy Group brand site | Turborepo + pnpm, Next.js 16 |
| [`adawy-group-links`](/repositories/adawy-group-links) | <Badge tone="group">Group</Badge> | The Group's own link-in-bio page — standalone, and an [open question](#the-third-case-and-the-open-question) | Next.js 16, standalone |
| [`aladawy-pack`](/repositories/aladawy-pack) | <Badge tone="client">Client</Badge> | Website for Aladawy Pack for Printing & Packaging | Next.js 16, standalone |
| [`aladawy-shop`](/repositories/aladawy-shop) | <Badge tone="client">Client</Badge> | Quote-request storefront for Aladawy Shop | Next.js 16 + Redux, standalone |
| [`jumeira`](/repositories/jumeira) | <Badge tone="client">Client</Badge> | Pre-launch film site for Jumeira Catering, Benghazi | Next.js 16, standalone |
| [`zad-alhkoul`](/repositories/zad-alhkoul) | <Badge tone="client">Client</Badge> | Site for Zad Al-Hkoul for Agricultural Crops | Next.js 16, standalone |
| [`baron-menu`](/repositories/baron-menu) | <Badge tone="client">Client</Badge> | QR menu for Baron — The Lord of Fried Chicken | Plain HTML/CSS/JS, no build |
| [`adawy-technical-docs`](/repositories/adawy-technical-docs) | <Badge tone="internal">Internal</Badge> | This handbook | Nextra 4 |

## The two worlds

**Group work** is Adawy Group's own brands — Adawy Group, Adawy Studio, Adawy
Pack, and the rest of the portfolio. Every brand **site** lives in one
monorepo, `adawy-platform`, on shared `@adawy/*` packages. This is where
[ADR-001](/architecture/adrs) and [ADR-008](/architecture/adrs) apply, and it is
the only place they apply.

**Client work** is software the department builds *for other businesses*.
Aladawy Pack, Aladawy Shop, Jumeira Catering, Zad Al-Hkoul and Baron are
customers of the department, not Group brands. Each gets **its own standalone
repository**, deliberately not coupled to `@adawy/*` — see
[ADR-010](/architecture/adrs) for why, and
[Client Projects](/how-we-work/client-projects) for how to run one.

Both worlds share the department's *standards* — TypeScript strict, bilingual
and RTL from day one, accessibility, the same review and deployment
discipline. What they do not share is **code**.

### The third case, and the open question

[`adawy-group-links`](/repositories/adawy-group-links) is Adawy Group's own
property in a standalone repository, which is neither pattern. It is a
one-screen microsite rather than a brand site, and the repository itself
records no reason for sitting outside the monorepo.

Until that is decided, read the rule as stated above — **Group brand *sites*
live in the monorepo** — and treat the microsite as a single open case, not a
precedent. Starting a new Group property outside the platform is still an
[ADR-010](/architecture/adrs) decision, not a default.

<Callout type="warning">
  **Adawy Pack and Aladawy Pack are two different companies**, one letter
  apart.

  - **Adawy** Pack is a Group brand. It lives at `apps/pack` inside
    `adawy-platform`.
  - **Alad**awy Pack (Aladawy Pack for Printing & Packaging, est. 1971) is a
    client. It lives in the `aladawy-pack` repository.

  They have different owners, different domains, different codebases, and
  different rules. When someone says "the pack site", ask which one.
</Callout>

## Which repo do I clone?

| The work | The repo |
| -------- | -------- |
| Anything on adawygroup.com | `adawy-platform` → `apps/group` |
| A shared component, token, or motion primitive | `adawy-platform` → `packages/` |
| A new Adawy Group brand site | `adawy-platform` → a new `apps/<brand>` ([guide](/guides/launch-new-brand)) |
| The Group's link-in-bio page | `adawy-group-links` |
| The Aladawy Pack website | `aladawy-pack` |
| The Aladawy Shop storefront | `aladawy-shop` |
| The Jumeira Catering site | `jumeira` |
| The Zad Al-Hkoul site | `zad-alhkoul` |
| Baron's menu — a price, an item, a photo | `baron-menu` |
| Fixing this handbook | `adawy-technical-docs` |

If you are new, clone [`adawy-technical-docs`](/repositories/adawy-technical-docs)
first. It is the smallest repo, it builds in under a minute, and getting it
running proves your toolchain before anything harder depends on it.

## Start with the repo you have been given

<Cards num={3}>
  <Cards.Card title="adawy-platform" href="/repositories/adawy-platform" arrow>
    The monorepo. One WebGL canvas, three locales, eight shared packages.
  </Cards.Card>
  <Cards.Card title="adawy-group-links" href="/repositories/adawy-group-links" arrow>
    One screen, two locales, five tests — and an open placement question.
  </Cards.Card>
  <Cards.Card title="aladawy-pack" href="/repositories/aladawy-pack" arrow>
    A monochrome marketing site with a surface system worth learning.
  </Cards.Card>
  <Cards.Card title="aladawy-shop" href="/repositories/aladawy-shop" arrow>
    A storefront where nothing has a price. Quote by grade, not checkout.
  </Cards.Card>
  <Cards.Card title="jumeira" href="/repositories/jumeira" arrow>
    One pinned scroll carrying three scrubbed films. The hardest client build.
  </Cards.Card>
  <Cards.Card title="zad-alhkoul" href="/repositories/zad-alhkoul" arrow>
    English-default, `src/`-rooted, and a three-layer design system.
  </Cards.Card>
  <Cards.Card title="baron-menu" href="/repositories/baron-menu" arrow>
    No framework, no build step, on purpose. Edit one file to change the menu.
  </Cards.Card>
  <Cards.Card title="adawy-technical-docs" href="/repositories/adawy-technical-docs" arrow>
    This handbook. The best first clone and the best first pull request.
  </Cards.Card>
</Cards>

## What the repos actually share

Moving between repos is cheap because they agree on most things. They do not
agree on all of them, and a rule you assume is universal is worse than one you
know is not — so here is the honest matrix.

| | Package manager | AGENTS.md | CI | Tests | Bilingual |
| --- | --- | --- | --- | --- | --- |
| `adawy-platform` | pnpm 10.33 | ✅ | ✅ | ✅ | en · ar · fr |
| `adawy-group-links` | pnpm 11.5 | ⚠️ stub | ✅ | ✅ | en · ar |
| `aladawy-pack` | pnpm 11.4 | ❌ | ✅ | ✅ | en · ar |
| `aladawy-shop` | pnpm | ✅ | ❌ | ✅ | ar · en |
| `jumeira` | pnpm 11.5 | ✅ +4 more | ⚠️ manual | ✅ | ar · en |
| `zad-alhkoul` | pnpm 11.5 | ⚠️ stub | ✅ | ❌ | en · ar |
| `baron-menu` | — none | ❌ | ❌ | ❌ | en · ar |
| `adawy-technical-docs` | **npm** | ❌ | ✅ | ❌ | English only |

**What is genuinely universal:**

- **`main` is the deployable branch.** Work lands through reviewed pull
  requests ([git workflow](/how-we-work/git-workflow)).
- **A `README.md`** covering what the project is, how to run it, and what
  ships. The README is the tour; `AGENTS.md` is the contract.
- **Bilingual with real RTL** in every product repo — `baron-menu` included —
  with every human-readable string in a message file rather than in a component
  ([i18n & RTL](/standards/i18n-rtl)). This handbook is the exception, English
  only by decision ([ADR-007](/architecture/adrs)).
- **Prettier settles formatting.** Where CI exists it gates on it; formatting is
  never a review conversation.

**Where the gaps are, and what they mean:**

- **`aladawy-pack` and `baron-menu` have no `AGENTS.md`**, and in
  `adawy-group-links` and `zad-alhkoul` the file holds only the block Next.js
  generates — the real constraints live in an untracked local notes file. If
  you are the next person into one of those repos, writing it is the
  highest-value small PR
  available ([working with AI](/how-we-work/working-with-ai)).
- **`aladawy-shop` has no CI at all.** It has tests and it has a `test` script;
  nothing runs them on a push. Its four gates are yours to run by hand until
  that is fixed.
- **`jumeira`'s CI exists but never fires by itself.** The workflow is
  `workflow_dispatch`-only because the account's Actions minutes are spent, so
  an automatic trigger would queue a run that never starts. In practice its
  gates live on a developer's machine exactly like `aladawy-shop`'s — start it
  from the Actions tab, or run all five before you push
  ([deployment](/how-we-work/deployment#client-projects-deploy-the-same-way)).
- **`jumeira` splits its contract across five files.** `AGENTS.md` is the entry
  point, and `SECTIONS.md`, `CHROME.md`, `FILMS.md` and `PRODUCT.md` carry what
  is scoped to one place. Read the one that covers what you are touching
  ([why](/how-we-work/working-with-ai#every-repo-carries-an-agentsmd)).
- **`zad-alhkoul` has no test framework.** Its locale parity is guarded by a
  standalone Node script instead ([why that is not the same
  thing](/repositories/zad-alhkoul#testing-what-is-actually-there)).
- **The CI job is not called the same thing everywhere** — `verify` here and in
  `aladawy-pack`, `checks` in `jumeira`, split across two workflows in
  `zad-alhkoul`. Read the repo's own workflow before quoting a job name in a
  PR.

<Callout type="info">
  These gaps are the current state, not the target. The target is the left-hand
  column of that table filled in everywhere it applies. Closing one is a
  legitimate Maintenance card ([the board](/how-we-work/the-board)).
</Callout>

## What is deliberately *not* a repo

- **No per-brand repositories for Group brand sites.** Adawy Studio and Adawy
  Pack are apps inside `adawy-platform`, not repos of their own. Splitting them
  would recreate the ×38 maintenance problem the monorepo exists to solve.
- **No backend repository yet.** The Phase-2 NestJS service joins the platform
  monorepo as `apps/api` when it is built ([ADR-009](/architecture/adrs)).
- **No shared package repository.** `@adawy/*` are workspace packages inside
  the monorepo, published nowhere and versioned with the apps that use them.
- **No shared client-project package.** The repeated patterns —
  `lib/enquiry.ts`, the content/messages split, the publishing switch — are
  documented here rather than packaged, because a client's deliverable has to
  be complete on its own ([ADR-010](/architecture/adrs)).
