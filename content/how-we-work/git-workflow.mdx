---
title: Git Workflow
---

# Git Workflow

{/* TODO(content): The real workflow:
    - branching model (e.g. trunk-based / GitHub flow), branch naming
    - commit message convention, with examples
    - PR size guidance, when to rebase vs merge
    - protected branches and merge rules */}

> 🚧 **Stub** — document the branching model, naming, and merge rules.
