---
title: Dev Environment Setup
---

# Dev Environment Setup

{/* TODO(content): Per-project setup. For each repo:
    - prerequisites (Node version via nvm/fnm, package manager)
    - clone → install → env vars (where to get them) → run dev → verify
    - common setup errors and fixes */}

> 🚧 **Stub** — step-by-step environment setup for each active project.
